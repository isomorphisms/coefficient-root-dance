# Third-party and reused material

`LICENSE` applies to copyrightable material in this repository only where the repository's contributors have authority to grant that license. Third-party dependencies, platform components, and separately identified material retain their own licenses.

## Wegert reuse

The Android shell intentionally reuses the basic NativeActivity, input, EGL, and OpenGL ES approach already used in `isomorphisms/wegert`. Wegert is being licensed under the same `GPL-3.0-or-later` policy, so that internal reuse has a consistent licensing path.

- https://github.com/isomorphisms/wegert

This note does not assert ownership of any material that may itself have originated outside those repositories; file-specific or upstream notices take precedence.

## Platform and toolchain

Android SDK/NDK components, Gradle, CMake, system libraries, and OpenGL ES interfaces are external to this repository and remain under their upstream terms.
